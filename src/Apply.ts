type ApplyAlias = {
  alias?: boolean
}

type ApplyAs = {
  as?: boolean
}

type ApplyPrefix = {
  prefix?: boolean
}

type ApplyQuote = {
  quote?: boolean
}


export type {
  ApplyAlias,
  ApplyAs,
  ApplyPrefix,
  ApplyQuote
}
